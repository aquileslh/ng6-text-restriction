import { AlphaMxDirective } from './alpha-mx.directive';

describe('AlphaMxDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaMxDirective();
    expect(directive).toBeTruthy();
  });
});
